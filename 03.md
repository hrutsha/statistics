import
```
from numpy import np
from matplotlib.pyplot as plt
from seaborn as sns
from statistics import multimode
```

```
data=np.array([])
```
bins
```
bins = np.arrange(starting_point,ending_point,difference)
```
descriptive statistics
```
mean_score=np.mean(data)
median_score=np.median(data)
mode_score=multimode(data)
```
histogram
```
plt.figure(figsize=())
plt.hist(data,bins=bins,edgecolor='black',alpha=0.7)
plt.xlabel("")
plt.ylabel("")
plt.xticks(np.arrange())
plt.show()
```
using seaborn
```
plt.figure(figsize=())
sns.kdeplot(data,color='green',linewidth=2,fill=True,alpha=.4,label="")
plt.title("")
plt.xlabel("")
plt.ylabel("")
plt.xticks(np.arrange())
plt.show()
```








